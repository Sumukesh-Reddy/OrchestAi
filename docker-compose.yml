version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: orchestai-backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongodb:27017/orchestai
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=super_prod_secret_sign_key_please_change_me
      - JWT_REFRESH_SECRET=super_prod_secret_refresh_key_please_change_me
      - GEMINI_API_KEY=your_gemini_api_key_here
      - GEMINI_MODEL=gemini-2.0-flash
      - LOG_LEVEL=info
      - LOG_DIR=logs
    depends_on:
      - mongodb
      - redis
    volumes:
      - backend-logs:/usr/src/app/logs
    restart: always

  mongodb:
    image: mongo:6.0
    container_name: orchestai-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db
    restart: always

  redis:
    image: redis:7.0-alpine
    container_name: orchestai-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    restart: always

volumes:
  backend-logs:
  mongodb-data:
  redis-data:
